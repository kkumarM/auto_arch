version: '3.8'
services:
  myservice:
    build: ./myservice
    environment:
      - DATABASE_URL=postgresql://admin:secret_password@my-cloud-db.aws.com:5432/production_db
  api_gateway:
    build: ./api_gateway
    ports:
      - '80:80'
    depends_on:
      - myservice
