events {}
http {
upstream microservice_upstream { server microservice:8000; }
    server {
        listen 80;
    location /microservice/ {
        proxy_pass http://microservice_upstream/;
    }
    }
}